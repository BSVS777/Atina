# Application / Docente / Ports — DO-01

Interfaces (puertos) que el dominio necesita y que `app/` implementa:
`DocenteRepository`, `AuditLogRepository`.

Solo tipos de PHP puro / Domain aquí — ninguna interfaz debe referenciar
Eloquent.

Estado: no iniciado.
